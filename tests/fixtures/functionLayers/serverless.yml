service: service
provider:
  name: aws

layers:
  testLayer:
    path: testLayer
    description: 'test layer'

package:
  exclude:
    - extra_layers/**

functions:
  foo:
    handler: index.handler
    layers:
      - { Ref: TestLayerLambdaLayer }
  bar:
    handler: index.handler
  fooBar:
    handler: index.handler
    reservedConcurrency: 1
