service: service
provider:
  name: aws

functions:
  foo:
    handler: index.handler
    vpc:
      securityGroupIds:
        - sg-0a0a0a0a
      subnetIds:
        - subnet-01010101
    fileSystemConfigs:
      - localMountPath: /mnt/path
        arn: arn:aws:elasticfilesystem:region:111111111111:access-point/fsap-a1a1a1a1a1a1a1a1a
