**/node_modules
**/dist
# **/.env # TODO: Our tests use many .env files, so we cannot ignore them ATM.
**/.serverless
# Local-only capture-first baselines for offline integration tests; embed
# machine-specific data (absolute paths, ephemeral ports, real account IDs).
**/.captured/
.DS_Store
.idea

# Snyk Security Extension - AI Rules (auto-generated)
.agent/rules/snyk_rules.md
