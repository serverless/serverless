service: eslam-serverless-dashboard-demo

tenant: eahefnawy
app: framework

provider:
  name: aws
  runtime: nodejs6.10

functions:
  hello:
    handler: handler.hello
    events:
      - user.created
      - http:
          path: test-again
          method: post
          cors: true
