service: aws-local

configValidationMode: error
frameworkVersion: '*'

provider:
  name: aws
  runtime: nodejs12.x

functions:
  hello:
    handler: handler.hello
    events:
      - http:
          path: local
          method: get
  callback:
    handler: callback.handler
