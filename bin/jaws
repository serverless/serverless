#!/usr/bin/env node

'use strict';

let JAWS      = require('../lib/Jaws'),
    JawsUtils = require('../lib/utils/index');

let Jaws = new JAWS(),
    argv = require('minimist')(process.argv.slice(2));

JawsUtils.execute(Jaws.command(argv));
