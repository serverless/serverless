service:
  name: aws-nodejs-typescript
# app and org for use with dashboard.serverless.com
#app: your-app-name
#org: your-org-name

# Add the serverless-webpack plugin
plugins:
  - serverless-webpack

provider:
  name: aws
  runtime: nodejs10.x

# setup a basic VPC configuration
# vpc: true

functions:
  hello:
    handler: handler.hello
    events:
      - http:
          method: get
          path: hello
