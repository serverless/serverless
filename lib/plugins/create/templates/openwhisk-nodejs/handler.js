'use strict';

function main(params) { // eslint-disable-line no-unused-vars
  return { message: 'Go Serverless v1.0! Your function executed successfully!', params };
}

// You can add more handlers here, and reference them in serverless.yaml
