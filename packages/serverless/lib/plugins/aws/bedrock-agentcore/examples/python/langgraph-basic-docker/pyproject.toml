[project]
name = "langgraph-basic-agent"
version = "1.0.0"
description = "Minimal LangGraph agent with built-in tools"
dependencies = [
    "bedrock-agentcore>=1.3.0",
    "langgraph>=1.0.8",
    "langchain>=1.2.10",
    "langchain-aws>=1.2.5",
    "langchain-core>=1.2.13",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
