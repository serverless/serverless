[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "langgraph-code-interpreter-agent"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "bedrock-agentcore>=0.1.0",
    "langchain>=0.3.0",
    "langchain-aws>=0.2.0",
    "langgraph>=0.2.0",
]
