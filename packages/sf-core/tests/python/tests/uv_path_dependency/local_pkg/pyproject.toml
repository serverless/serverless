[project]
name = "my-path-package"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
  "flask==3.1.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/my_path_package"]
