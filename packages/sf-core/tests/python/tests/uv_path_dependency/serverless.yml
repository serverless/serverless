service: sls-py-req-test

provider:
  name: aws
  runtime: python3.13

custom:
  pythonRequirements:
    useUv: true
    installer: uv
    useStaticCache: false

package:
  patterns:
    - '!**/*'
    - 'handler.py'

functions:
  hello:
    handler: handler.hello
