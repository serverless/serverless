service: sls-py-func-level-zip-mixed

provider:
  name: aws
  runtime: python3.13

custom:
  pythonRequirements:
    dockerizePip: false
    zip: ${env:zip, true}
    pythonBin: ${env:pythonBin, 'python3.13'}

functions:
  individualFunction:
    handler: handler_individual.hello
    package:
      individually: true

  sharedFunction:
    handler: handler_shared.hello
