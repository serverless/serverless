[project]
name = "my-local-package"
version = "1.0.0"
requires-python = ">=3.13"
dependencies = [
  "flask==3.1.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/my_local_package"]
