service: compose-devtest-api
provider:
  name: aws
  region: us-east-1
  runtime: nodejs24.x
  environment:
    LOCAL_TOPIC_ARN: ${param:localTopicArn}
    SHARED_TOPIC_ARN: ${param:sharedTopicArn}
functions:
  hello:
    handler: handler.hello
    events:
      - httpApi: GET /hello
