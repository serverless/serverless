service: state-resolver-compose-b

provider:
  name: aws
  runtime: nodejs20.x
  lambdaHashingVersion: 20201221

functions:
  hello:
    handler: handler.hello
    environment:
      apiUrl: ${param:apiUrl}
