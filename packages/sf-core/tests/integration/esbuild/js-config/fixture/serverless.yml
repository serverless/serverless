service: esbuild-js-config

build:
  esbuild:
    configFile: ./esbuild.config.js

provider:
  name: aws
  runtime: nodejs20.x

functions:
  hello:
    handler: src/hello.handler
  hello2:
    handler: src/hello2.handler
