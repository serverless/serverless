service: domains-http-api

provider:
  name: aws
  runtime: nodejs22.x
  domain:
    name: http-api.sls-domains-tests.click
    apiType: http
    endpointType: regional

functions:
  hello:
    handler: src/hello.handler
    events:
      - httpApi:
          path: /
          method: get
