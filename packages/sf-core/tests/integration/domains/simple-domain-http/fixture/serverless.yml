service: domains-simple-domain-http

provider:
  name: aws
  runtime: nodejs22.x
  domain: sls-domains-tests.click

functions:
  hello:
    handler: src/hello.handler
    events:
      - httpApi:
          path: /
          method: get
