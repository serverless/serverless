service: compose-b

frameworkVersion: '4'

provider:
  name: aws
  runtime: nodejs22.x
  lambdaHashingVersion: 20201221

functions:
  hello:
    handler: handler.hello
    environment:
      apiUrl: ${param:apiUrl}
