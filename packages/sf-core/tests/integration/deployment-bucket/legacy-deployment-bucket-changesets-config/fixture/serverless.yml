# cs stand for changesets. Shortened to avoid role name limit error.
service: legacy-bucket-cs-yml

provider:
  name: aws
  runtime: nodejs22.x
  deploymentMethod: changesets
  enableLegacyDeploymentBucket: true

functions:
  hello:
    handler: handler.hello
