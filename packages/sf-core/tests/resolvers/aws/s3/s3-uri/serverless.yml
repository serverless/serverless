org: serverlesstestaccount
app: resolvers-integration-test
service: resolvers-legacy-test

stages:
  default:
    resolvers:
      awsAccount1:
        type: aws

functions:
  hello:
    handler: handler.hello
    description: ${awsAccount1:s3:s3://resolvers-integration-test/test.txt}
