org: serverlesstestaccount
app: resolvers-integration-test
service: resolvers-legacy-test

functions:
  hello:
    handler: handler.hello

    # module.exports.value = 'value' (named export on module namespace)
    name: ${file(foo.cjs):value}
    description: ${file(foo.cjs)}
