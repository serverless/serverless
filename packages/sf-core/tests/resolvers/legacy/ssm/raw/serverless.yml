org: serverlesstestaccount
app: resolvers-integration-test
service: resolvers-legacy-test

functions:
  hello:
    handler: handler.hello
    name: ${ssm:/resolvers/sample-json-param}
    description: ${ssm(raw):/resolvers/sample-json-param}
    array: ${ssm:/resolvers/sample-list-param}
    rawArray: ${ssm(raw):/resolvers/sample-list-param}
