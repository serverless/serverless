org: serverlesstestaccount
app: resolvers-integration-test
service: resolvers-legacy-test

functions:
  hello:
    handler: handler.hello
    name: ${ssm:/resolvers/sample-secure-param}
    description: ${ssm(noDecrypt):/resolvers/sample-secure-param}
    otherRegion: ${ssm(eu-west-1, noDecrypt):/resolvers/sample-secure-param-eu-west-1}
