org: serverlesstestaccount
service: resolvers-fallback-test

provider:
  stage: ${opt:stage, env:NAME}

stages:
  john:
    resolvers:
      resolver-1:
        type: example
        option1: foo

functions:
  hello:
    handler: handler.hello
    description: ${resolver-1:key} ${sls:stage}
