org: serverlesstestaccount
app: resolvers-integration-test
service: resolvers-param-test

stages:
  default:
    params:
      dashboard-param: local-value

functions:
  hello:
    handler: handler.hello
    name: hello-local-value
