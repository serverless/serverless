service: resolvers-credentials-test

stages:
  default:
    resolvers:
      acc-2:
        type: aws
        profile: acc2

provider:
  name: aws
  profile: acc1

functions:
  hello:
    handler: handler.hello
    description: 123456789012 acc1-value
