service: resolvers-credentials-test

stages:
  default:
    resolvers:
      aws:
        type: aws

provider:
  name: aws
  profile: acc1

functions:
  hello:
    handler: handler.hello
    description: 377024778620 acc1-value
