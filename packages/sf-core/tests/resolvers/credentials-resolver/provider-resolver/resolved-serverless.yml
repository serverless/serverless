service: resolvers-credentials-test

stages:
  default:
    resolvers:
      aws-acc1:
        type: aws
        profile: acc1
      aws-acc2:
        type: aws
        profile: acc2

provider:
  name: aws
  resolver: aws-acc1

functions:
  hello:
    handler: handler.hello
    description: 123456789012 acc1-value
