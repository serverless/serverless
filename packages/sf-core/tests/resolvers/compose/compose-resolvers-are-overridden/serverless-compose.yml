org: ${env:ORG}
app: resolvers-integration-test
service: resolvers-compose-test

stages:
  default:
    resolvers:
      compose-resolver:
        type: aws

services:
  service-a:
    path: service-a
  service-b:
    path: service-b
    params:
      apiUrl: ${service-a.apiUrl}
  service-c:
    path: service-c
